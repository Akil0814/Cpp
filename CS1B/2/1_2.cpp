int Digit(int number, int n)
{
    int arr[15];
    while(number>0)
    {
        number=number%10
        arr[i]=number
    }

}

int main()
{
    int x=0;
    int index=0;

    cout<<"Enter a number:";
    cin>>x;
    cout<<"Enter the digit you looking for"
    cin>>index;

    cout<<Digit(x,index);

    return 0;
}